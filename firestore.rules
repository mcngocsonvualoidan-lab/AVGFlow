rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // DEVELOPMENT MODE: Allow all access to ensure Admin Panel (localStorage based) works without Firebase Auth.
      // TODO: Implement Firebase Auth for Admin Panel for production security.
      allow read, write: if true;
    }
  }
}
